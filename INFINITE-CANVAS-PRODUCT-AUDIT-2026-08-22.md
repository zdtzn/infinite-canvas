# 无限画境产品全面审查报告

审查日期：2026-08-22
审查版本：`v0.9.0`
审查提交：`3338bbe1acfdcdc19afa9e9eca712f243bab1533`
生产环境：`https://mingche.click`
审查性质：正式上线前产品级审查，只审查，不修改业务代码

---

## 审查方法与证据边界

本报告基于当前提交重新检查项目结构、前后端实现、SQLite 数据层、AI 调用链、任务系统、资产系统、权限系统、主要页面和生产响应，不复用旧报告结论。

已完成的验证：

- `bun run typecheck`：通过。
- `bun test ../server src`：466 项通过，0 项失败。
- `bun run build`：通过。
- 本地 `/health` 与生产 `https://mingche.click/health`：HTTP 200，版本 `v0.9.0`。
- 生产响应已启用 HTTPS、HSTS、CSP、COOP、CORP、`nosniff` 和 `DENY`。
- SQLite 处于 WAL 模式；本机每日备份已启用，保留 14 份；审查时磁盘剩余约 16.5 GB。
- 对 1440px 桌面和 390px 手机宽度检查首页、丹青台、画布、灵彩、问道台、商品幻境、藏卷阁、功法楼和修炼相关页面；主模块未发现整页横向溢出。
- 本地已登录状态下，首页首次进入问道台的路由交互约 62-157ms。开发服务器资源时延不作为生产性能结论。
- 生产域名当前由 Caddy 直接响应，未观察到 `CF-Ray` 或 `CF-Cache-Status`；因此不能把 Cloudflare CDN、WAF 或边缘缓存视为已经生效。

证据限制：

- 300、500、1000 节点画布没有现成自动性能基准，相关结论标记为“待验证”。
- 未对全部第三方 AI 渠道实施真实付费生成压力测试；渠道协议结论以能力表、请求校验、任务恢复代码和既有测试为依据。
- 未进行外部渗透测试、供应链 SCA 或真实多用户并发压测；安全和容量结论属于代码级与配置级审查。

---

# 一、项目整体评价

当前版本已经不是原型，而是一个功能较完整、可以公开使用的中文 AI 创作工具。核心链路已经形成：用户可在丹青台生成图片，把结果带入无限画布继续组织和加工，再进入灵彩调色、藏卷阁沉淀资产；问道台、商品幻境和修炼权限体系也已经具备真实业务逻辑，不是只有界面。

技术底座总体稳健。React 19 + Vite + Bun + SQLite WAL 的单体架构适合当前用户规模，部署简单，数据与文件所有权边界明确；生图任务具备持久化、幂等、恢复、取消、部分成功和失败额度返还机制，明显优于多数个人项目常见的“前端直接请求上游”方案。

当前阻碍产品进入成熟商业化阶段的主要问题不是功能太少，而是关键模块之间的完成度不均衡：画布多端冲突仍需真实环境回归；商品套图和站内聊天虽已具备跨页面恢复能力，但后台续答与极端渠道场景仍需观察；移动端画布只达到有限可用；问道台缺少通用长期记忆；灵彩的“AI”命名高于实际能力。提示词库已经改为服务端索引，藏卷阁已经改为服务端分页，后续重点转为来源健康、搜索体验和大规模资产治理。

**总体结论：有条件上线，但应先修复唯一 P0，并把前五项 P1 作为首个稳定性迭代。**

---

# 二、产品定位评价

## 当前定位

“无限画境”已经从单一无限画布扩展为：

> 中文 AI 视觉创作空间：生成、组织、加工、沉淀和再次创作在同一套资产体系中完成。

这比“AI 生图网站”更有长期价值，也比把所有功能包装成修仙游戏更符合真实用户需求。

## 最值得强化的差异化

真正有机会成为核心竞争力的不是模块数量，而是：

> **会理解创作上下文、保留生成血缘、可跨模块继续加工的中文 AI 创作空间。**

具体表现应是：

- 一张图片能追溯来自哪个提示词、模型、参考图、画布节点和生成任务。
- 丹青台、画布、灵彩、商品幻境、藏卷阁使用同一资产，不要求重复上传。
- 问道台能读取用户选中的画布节点、项目目标和历史版本，帮助继续创作。
- 修炼体系只表达身份、额度、并发和能力解锁，不主导产品结构。

## 不应走的方向

- 不应继续堆独立工具入口，让产品变成“功能大杂烩”。
- 不应增加商城、抽卡、排行榜、宗门社交等重游戏化系统。
- 不应为了对标专业修图软件而在浏览器主线程实现完整 RAW 工作流。
- 不应给所有模型统一塞入 Negative Prompt、seed、steps 等参数，应按模型能力动态展示。

---

# 三、现有功能地图

图例：✅ 已实现；🟡 部分实现；❌ 缺失；⚠️ 已实现但存在问题。

## 3.1 无限画布

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 画布缩放、平移、适配视图 | ✅ | 桌面端核心操作完整。 |
| 节点拖动、多选、框选 | ✅ | 多节点拖动与分组关系已处理。 |
| 复制、粘贴、删除 | ✅ | 节点和关联连线可处理；生成中节点删除存在任务生命周期问题。 |
| 撤销、重做 | ⚠️ | 有 50 条历史上限和 180ms 合并；缩放节点未显式形成单次事务。 |
| 图片、文本、配置、音视频节点 | ✅ | 节点类型较完整，支持扩展节点。 |
| 连线与参考关系 | ✅ | 支持来源/目标连接和引用解析。 |
| 分组 | ✅ | 可创建组并处理拖入、拖出。 |
| Mini Map | ✅ | 已实现。 |
| 视口裁剪与缩略图 | ✅ | 已按视口筛选节点，并支持缩略图补建。 |
| 批量结果布局 | ✅ | 多图结果和拆图结果有自动排列。 |
| 通用自动布局 | 🟡 | 有局部批次布局，不是完整的一键整理画布。 |
| 节点层级面板 | ❌ | 缺少明确图层列表和可视化层级管理。 |
| 锁定、隐藏 | ❌ | 专业画布常用能力缺失。 |
| 对齐、等距分布、吸附参考线 | ❌ | 多节点精确排版成本较高。 |
| 右键菜单、快捷键 | ✅ | 基础操作具备。 |
| 触摸屏、双指缩放 | ⚠️ | 页面能显示，但节点拖动、缩放和连线仍大量依赖鼠标事件。 |
| 多端同步 | ⚠️ | 有 revision 和墓碑同步，但冲突不保存副本。 |
| 版本分支、版本比较 | ❌ | 只能线性 Undo，无法保留创作分支。 |

## 3.2 AI 生图与丹青台

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 文生图 | ✅ | 服务端任务化并持久化。 |
| 图生图、参考图 | ✅ | 支持上传、粘贴、预览和模型能力限制。 |
| 多参考图 | 🟡 | 按渠道和模型能力动态限制。 |
| Prompt 优化 | ✅ | 使用后台文本模型，不在前端暴露模型选择。 |
| Negative Prompt | 🟡 | 未形成通用控件；应继续按模型能力决定是否展示。 |
| 比例、尺寸、质量、格式、数量 | ✅ | 前端能力表与服务端二次校验并存。 |
| 新渠道保守能力 | ✅ | 未识别模型默认 1:1、1 张参考图、1 张输出，避免错误放开。 |
| 任务状态、刷新恢复、取消、重试 | ✅ | 任务系统完整度较高。 |
| 部分成功与失败退款 | ✅ | 额度预占、结算和退款在事务中完成。 |
| SADAI/UU 临时结果归档恢复 | ✅ | 支持延迟归档和结果恢复。 |
| 生成结果继续编辑 | ✅ | 可进入画布、灵彩、下载、保存和再次使用。 |
| 删除节点同步取消生成 | ⚠️ | 页面离开会取消请求，删除单个生成节点不会取消对应请求。 |

## 3.3 灵彩

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 亮度、对比度、曝光、高光、阴影 | ✅ | 浏览器非破坏式参数处理。 |
| 饱和度、色温、色调、HSL | ✅ | 基础调色链完整。 |
| LUT 预设与 `.cube` 应用 | ✅ | 已有解析、应用和测试。 |
| 吸色与色彩分析 | ✅ | 支持取色和画面色彩统计。 |
| 参考调色 | ✅ | 基于源图与参考图统计差异推导参数。 |
| “AI 炼彩” | ⚠️ | 实际为本地像素分析和规则推荐，不是视觉模型。 |
| 曲线 | ❌ | UI 和渲染已移除，但数据结构仍保留。 |
| Before / After | ✅ | 支持原图对比。 |
| 撤销、重做、草稿同步 | ✅ | 草稿有服务端存储和数量上限。 |
| 原尺寸导出 | ⚠️ | 大图在主线程分块逐像素处理，可能冻结交互。 |
| 局部蒙版调色 | ❌ | 当前为全图处理。 |
| 抠图 | ❌ | 已按产品决定移除，不应作为当前缺陷。 |

## 3.4 问道台与斗气人生

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 多会话、历史、删除、导入导出 | ✅ | 会话持久化和操作保护较完整。 |
| 流式回答、停止、重试、继续、编辑 | ✅ | 用户消息先乐观显示，异常状态可恢复。 |
| 图片提问 | ✅ | 附件归属和数量有校验。 |
| 角色预设与会话绑定 | ✅ | `presetId` 已绑定会话，避免角色互相污染。 |
| 自定义人格 | ✅ | 用户偏好服务端保存，可跨设备。 |
| 通用长期记忆 | 🟡 | 普通问道台只发送最近上下文；斗气人生已有摘要、事实、事件和目标。 |
| 离开页面后台继续回答 | ❌ | 卸载页面会中止当前回答。 |
| 移动端会话抽屉 | 🟡 | 可用，但顶部操作和输入区拥挤。 |

## 3.5 商品幻境

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 商品上传与项目管理 | ✅ | 项目、分析、规划和生成记录已持久化。 |
| AI 商品识别与卖点提炼 | ✅ | 通过后台视觉文本模型分析。 |
| 推荐主图和六张详情页 | ✅ | 一键规划和确认生成已实现。 |
| 单页失败重试 | ✅ | 可针对失败页面重试。 |
| 手动进入藏卷阁 | ✅ | 不会自动污染作品库。 |
| 境界能力控制 | ✅ | 复用 Capability Key。 |
| 套图后台持续生成 | ❌ | 当前由前端顺序循环维持，刷新后剩余页面停止。 |
| 大批量商品项目 | 🟡 | 有项目上限，尚非服务端批任务工作流。 |

## 3.6 功法楼、藏卷阁、用户与后台

| 功能 | 状态 | 审查结论 |
|---|---|---|
| 多提示词来源 | ✅ | 内置、自定义、管理员配置、缓存与失败来源隔离均存在。 |
| 标签分类、搜索、分页 | ✅ | 服务端提示词索引按关键词、来源、分类、标签和页码查询；浏览器只读取当前页。 |
| 提示词来源刷新 | ✅ | 支持单源和全部刷新。 |
| 藏卷阁上传、搜索、筛选、删除 | ✅ | 图片、视频、文本资产统一。 |
| 藏卷阁缩略图补建 | ✅ | 老图片可后台补缩略图。 |
| 藏卷阁分页 | ✅ | 登录后首屏读取第一页，继续操作时按页加载；WebDAV 主动同步仍会显式读取完整资产集。 |
| 用户登录与账号隔离 | ✅ | Session、用户资源所有权和管理员权限有效。 |
| 修为、境界、能力、额度、并发 | ✅ | 配置和账本完整。 |
| 管理后台 | ✅ | 用户、渠道、修炼规则、日志和提示词来源可管理。 |
| 自助修改密码、找回密码、设备管理 | ❌ | 账号安全闭环未完成。 |

---

# 四、严重问题

## 【AUD-001】

【等级】🔴 P0
【类别】数据安全 / 多端同步
【问题名称】画布同步冲突只警告，不保留本地冲突副本
【问题位置】`web/src/hooks/use-project-server-sync.ts:74`，冲突分支约 `:84`
【当前行为】保存遇到远端 revision 更高时，仅提示“当前本地修改未覆盖云端版本”并结束；没有保存副本、下载快照、冲突合并或显式放弃本地修改。
【为什么有问题】冲突发生后，本地修改仍暂存在当前内存，但后续重新加载或远端同步可能把当前内容替换为云端版本。用户看到了警告，却没有可靠动作把自己的修改保存下来。
【实际影响】跨电脑、跨标签页或网络恢复时存在真实创作数据丢失路径，属于阻止无条件上线的问题。
【建议修改】冲突时自动创建“项目名 · 冲突副本 · 时间”并保存为新项目 ID；同时提供“查看云端版 / 保留本地副本 / 导出 JSON”三个动作。服务端保留最近 10 个 revision 快照。
【修改优先级】第 1 位。
【修改后收益】把不可逆覆盖变成可恢复冲突，显著提升用户对画布存储的信任。
【是否必须修改】是，上线前必须修改。

---

# 五、逻辑问题

## 【AUD-002】

【等级】🟠 P1
【类别】画布 / AI 任务生命周期
【问题名称】删除正在生成的节点不会中止对应生成请求
【问题位置】`web/src/pages/canvas/project.tsx:295`、`:318`、`:826`
【当前行为】生成请求保存在 `generationRequestsRef`；页面卸载会统一 abort，但 `deleteNodes` 只清理待上传图片，没有查找并中止被删除节点的生成请求。
【为什么有问题】用户认为删除节点等于取消该创作，但上游任务仍可能继续运行、扣费并返回结果。
【实际影响】产生无可见结果的 API 消耗、额度争议和“已删除为什么还在生成”的状态不一致。
【建议修改】在删除节点前收集目标节点和批次子节点，abort 对应 controller，并调用服务端任务取消；服务端不可取消时将任务标记为“结果不再回填”，保留扣费说明。
【修改优先级】第 2 位。
【修改后收益】节点生命周期与任务生命周期一致，减少无效成本。
【是否必须修改】建议上线前修改。

## 【AUD-003】

【等级】🟠 P1
【类别】商品幻境 / 长任务
【问题名称】商品套图由前端顺序循环生成，刷新后任务中断
【问题位置】`web/src/pages/product-lab/index.tsx:497`、`:539`
【当前行为】主图和详情页在页面内 `for...of` 顺序请求；关闭、刷新或离开页面后，尚未提交的页面不再继续。
【为什么有问题】“一键生成一套”是商品幻境核心承诺，不能依赖一个浏览器页面持续存活。
【实际影响】七张套图中途停止、用户不知道哪些还未提交、需要重复操作，且失败恢复粒度不稳定。
【建议修改】新增服务端 `product_batch_jobs` 或复用现有任务系统，保存计划项、状态、重试次数和结果；前端只提交一次批任务并订阅进度。
【修改优先级】第 3 位。
【修改后收益】套图可后台继续、刷新恢复、逐页重试，商品幻境从页面流程升级为可靠业务流程。
【是否必须修改】核心卖点正式推广前必须修改。

## 【AUD-004】

【等级】🟠 P1
【类别】问道台 / 会话生命周期
【问题名称】离开问道台会停止正在生成的回答
【问题位置】`web/src/pages/chat/index.tsx:172`
【当前行为】组件卸载或 `pagehide` 时执行 `abortOnLeave`；切换站内模块也会停止回答。
【为什么有问题】成熟聊天产品通常允许回答在后台继续，用户不应被迫停留在一个页面等待。
【实际影响】长回答、图片分析和慢模型场景下，用户无法并行使用丹青台或画布，降低整站联动价值。
【建议修改】把流式任务状态移到全局任务 Store 或服务端 Chat Run；切页后保持流连接或轮询结果，导航处显示“问道台正在回答”。只有用户点击停止、退出账号或会话被删除时中止。
【修改优先级】第 7 位。
【修改后收益】提高多任务使用效率，问道台真正成为全站助手。
【是否必须修改】建议首个上线迭代修改。

## 【AUD-005】

【等级】🟡 P2
【类别】画布 / Undo 语义
【问题名称】节点缩放没有显式作为单次历史事务
【问题位置】`web/src/pages/canvas/project.tsx:438`、`:1762`
【当前行为】历史依赖 180ms 防抖；拖动会设置 `historyPausedRef`，缩放开始和结束只改变 UI 状态，没有暂停历史并在结束时提交一次。
【为什么有问题】慢速调整或中途停顿超过 180ms 时，单次缩放可能形成多条 Undo。
【实际影响】用户按一次撤销不能回到缩放前，专业画布操作的可预测性下降。
【建议修改】在 resize start 保存快照并暂停历史，在 resize end 一次提交最终尺寸和位置。
【修改优先级】中。
【修改后收益】Undo 与用户动作一一对应。
【是否必须修改】否，但应在画布交互完善阶段修改。

## 【AUD-006】

【等级】🟡 P2
【类别】灵彩 / 产品语义
【问题名称】“AI 炼彩”名称与实际实现不一致
【问题位置】`web/src/features/color-alchemy/color-engine.ts:129`、`:205`，`web/src/pages/color-alchemy/index.tsx:229`
【当前行为】系统在浏览器提取明度、对比度、饱和度、色温等统计值，再按固定阈值推荐参数，没有调用视觉模型。
【为什么有问题】“AI”会让用户期待语义理解、主体识别和风格判断，当前能力更接近“智能自动调色”。
【实际影响】用户对结果质量预期过高，容易误认为后台模型没有生效。
【建议修改】短期改名为“智能炼彩”或“自动优化”，明确本地处理、免费且不上传；未来接入视觉模型后再使用“AI 场景调色”。
【修改优先级】中。
【修改后收益】预期与能力一致，同时把本地隐私和即时响应转化为优势。
【是否必须修改】建议修改文案，不必重做算法。

## 【AUD-007】

【等级】🟡 P2
【类别】灵彩 / 数据模型
【问题名称】曲线 UI 已移除，但曲线字段和迁移逻辑仍保留
【问题位置】`web/src/features/color-alchemy/types.ts:32`、`settings.ts:27`
【当前行为】`curves` 仍是 `ColorSettings` 必填字段并参与默认值、克隆、合并和迁移，但渲染引擎不应用曲线，界面也没有入口。
【为什么有问题】这是半成品遗留：数据看似支持，实际不可见也不生效。
【实际影响】增加草稿体积和维护成本，未来重新实现曲线时容易误判旧数据有效。
【建议修改】若近期不做曲线，执行版本化迁移移除字段；若计划重做，先标记实验字段并禁止对外导入导出。
【修改优先级】中低。
【修改后收益】数据模型与真实功能一致。
【是否必须修改】否。

## 【AUD-008】

【等级】🟡 P2
【类别】修炼体系 / 权限逻辑
【问题名称】未分类的新模型不会产生对应 `model.*` 权限要求
【问题位置】`server/modules/cultivation/policy.ts:53`
【当前行为】只识别 GPT-Image、Gemini、Flux 三类模型并生成模型能力 Key；其他新模型仅受通用生成能力限制。
【为什么有问题】管理员新增渠道和模型后，可能以为境界模型权限自动生效，实际上新模型没有专属门槛。
【实际影响】境界能力和后台配置不一致，未来按模型成本分层时可能绕过预期限制。
【建议修改】把模型权限 Key 绑定到管理员模型配置，而不是依赖模型名称字符串猜测；未配置时使用 `model.custom` 或明确“无模型门槛”。
【修改优先级】中。
【修改后收益】权限行为可解释、可配置，并适配未来新渠道。
【是否必须修改】在启用按模型分层收费前必须修改。

---

# 六、UX 问题

## 【AUD-009】

【等级】🟠 P1
【类别】首次使用 / 画布引导
【问题名称】空画布缺少明确的三项首步入口
【问题位置】空项目画布主视图和创建菜单
【当前行为】用户进入空画布后主要依赖工具栏、右键菜单或已有使用经验开始操作。
【为什么有问题】第一次使用者需要先理解节点、连接和生成面板，学习成本高于普通生图工具。
【实际影响】用户可能进入画布后不知道应该上传、生成还是创建文本，首次创作完成率下降。
【建议修改】空画布中心只显示“上传素材”“AI 生图”“创建文本”三个大入口；首个节点创建后自动消失，不增加长期干扰。
【修改优先级】第 8 位。
【修改后收益】降低首次使用成本，把画布能力转成明确动作。
【是否必须修改】建议上线前或上线首周修改。

## 【AUD-010】

【等级】🟠 P1
【类别】移动端 / 问道台
【问题名称】手机端问道台顶部与输入区过度拥挤
【问题位置】`web/src/pages/chat/index.tsx` 的移动布局
【当前行为】390px 宽度下，角色、会话和操作集中在顶部，输入框被附件和预设操作压缩。
【为什么有问题】聊天页的主任务是阅读和输入，辅助操作不应持续占据最窄区域。
【实际影响】长问题输入困难，误触概率增加，视觉焦点从对话内容转移到工具栏。
【建议修改】手机端将会话、角色、导入导出收进左右抽屉或底部菜单；输入区保持整行文本框，附件和角色改为图标按钮。
【修改优先级】高。
【修改后收益】接近 GPT、Gemini 等成熟聊天产品的移动交互。
【是否必须修改】移动端正式推广前必须修改。

## 【AUD-011】

【等级】🟡 P2
【类别】商品幻境 / 异常体验
【问题名称】服务未连通时加载骨架占据主流程，恢复动作不够直接
【问题位置】`web/src/pages/product-lab/index.tsx` 的初始化与错误状态
【当前行为】商品上下文、项目列表或能力信息加载失败时，大面积骨架和页面结构仍然存在，错误信息与重试动作没有取代主流程成为焦点。
【为什么有问题】用户无法判断是在加载、无权限、未配置模型还是服务异常。
【实际影响】错误场景下页面显得“卡住”，增加重复刷新和重复上传。
【建议修改】将初始化错误收敛为一块就地状态：明确原因、受影响能力、重试按钮和返回入口；已经有项目缓存时先展示旧项目，再提示数据可能过期。
【修改优先级】中。
【修改后收益】异常可理解、可恢复，减少空等。
【是否必须修改】否。

## 【AUD-012】

【等级】🟡 P2
【类别】跨模块 UX / 信息架构
【问题名称】各模块像独立子产品，跨模块下一步不够统一
【问题位置】丹青台、无限画布、灵彩、问道台、商品幻境、藏卷阁
【当前行为】各模块有不同标题密度、工具栏结构、空状态和结果动作；资产虽然互通，但用户需要自己理解何时去画布、灵彩或藏卷阁。
【为什么有问题】产品优势本应是跨模块继续创作，当前导航更像六个并列工具。
【实际影响】用户记住的是多个入口，而不是一条完整创作链，复访和资产积累动力不足。
【建议修改】统一结果操作顺序为“继续创作 / 进入画布 / 灵彩处理 / 保存藏卷阁 / 下载”；为所有资产使用同一来源标识和最近操作。
【修改优先级】高。
【修改后收益】强化“一个创作空间”而非“工具集合”的心智。
【是否必须修改】建议在第二阶段完成。

## 【AUD-013】

【等级】🟢 P3
【类别】功法楼 / 用户语言
【问题名称】普通用户直接看到 GitHub 仓库式来源名称
【问题位置】功法楼来源筛选与卡片来源信息
【当前行为】来源名称包含 `davidwu-gpt-image2-prompts`、`freestylefly/awesome-gpt-image-2` 等技术仓库名称。
【为什么有问题】普通用户更关心“海报、电商、摄影、动漫”等内容价值，而不是抓取自哪个仓库。
【实际影响】页面带有开发工具感，标签认知成本高。
【建议修改】前台使用产品化来源名和主题描述，仓库地址放在详情“内容来源”中；管理员后台保留技术标识。
【修改优先级】低。
【修改后收益】降低技术感，提升内容库的可读性。
【是否必须修改】否。

---

# 七、UI 问题

## 【AUD-014】

【等级】🟡 P2
【类别】视觉系统
【问题名称】模块视觉语言和组件密度不统一
【问题位置】无限画布、灵彩、问道台、藏卷阁、商品幻境
【当前行为】画布偏工作台，灵彩偏暗色修图器，商品幻境偏步骤页，藏卷阁首屏偏展示页，问道台偏聊天应用；圆角、背景层级、标题尺度、空状态占比和按钮风格存在差异。
【为什么有问题】功能差异可以存在，但导航、状态、命令和资产操作应遵循同一套视觉语法。
【实际影响】用户在模块间切换时需要重新理解界面，产品显得由多个项目拼接。
【建议修改】建立轻量设计基线：统一页面标题高度、工具栏高度、主按钮位置、空状态尺度、错误状态、资源卡片、抽屉和结果动作；东方感只用于文案、纹理和身份反馈。
【修改优先级】中高。
【修改后收益】提升商业产品一致性，不要求全面换肤。
【是否必须修改】否，但应持续执行。

## 【AUD-015】

【等级】🟡 P2
【类别】藏卷阁 / 移动视觉
【问题名称】手机端藏卷阁首屏展示区过大
【问题位置】`web/src/pages/assets/index.tsx` 的移动端首屏
【当前行为】标题和介绍占据较大高度，资产筛选与第一批内容下沉。
【为什么有问题】藏卷阁是高频资产工具，不是营销落地页，用户进入后主要目标是找到图片。
【实际影响】每次进入都需要额外滚动，资产越多越影响高频检索。
【建议修改】手机端将 Hero 压缩为单行标题、数量和搜索；上传与筛选吸附在顶部，桌面端可保留更完整氛围。
【修改优先级】中。
【修改后收益】首屏直接看到资产，提高重复使用效率。
【是否必须修改】否。

---

# 八、性能问题

## 【AUD-016】

【等级】🟠 P1
【类别】灵彩 / 主线程性能
【问题名称】原尺寸导出在主线程逐像素处理
【问题位置】`web/src/features/color-alchemy/renderer.ts:70`、`:98`
【当前行为】超过 400 万像素后虽然按 768 像素块处理，但双层循环没有让出主线程，也未使用 Worker 或 OffscreenCanvas。
【为什么有问题】分块只降低单次内存峰值，没有消除同步 CPU 占用。
【实际影响】4K、大尺寸电商图和多 LUT 场景下可能出现页面冻结、按钮失去响应和浏览器“页面无响应”。
【建议修改】将导出处理迁移到 Web Worker + OffscreenCanvas；不支持时每处理若干 tile 主动 yield，并显示可取消进度。
【修改优先级】第 4 位。
【修改后收益】保留原尺寸导出能力，同时避免拖垮整站交互。
【是否必须修改】大图导出作为正式能力前必须修改。

## 【AUD-017】

【等级】🟠 P1
【类别】功法楼 / 冷启动
【问题名称】首次无缓存时等待所有启用来源完成
【问题位置】`web/src/services/api/prompts.ts:136`
【当前行为】`Promise.all` 等待全部来源；虽然失败来源会被隔离、过期缓存会先返回旧数据，但首次没有缓存时，任意慢来源仍会拖慢首屏。
【为什么有问题】提示词库属于浏览内容，不应被最慢的数据源决定首次可用时间。
【实际影响】用户会看到部分内容长时间空白或加载失败，尤其在 GitHub 原始内容访问不稳定时。
【建议修改】服务端定时聚合并持久化；前端先返回内置精选和已完成来源，慢来源增量追加；为每个来源单独展示状态。
【修改优先级】第 5 位。
【修改后收益】首屏稳定，单一外部来源不再影响整个功法楼。
【是否必须修改】建议上线首个性能迭代修改。

## 【AUD-018】

【等级】🟡 P2
【类别】功法楼 / 数据规模
【问题名称】提示词先全量聚合，再在浏览器过滤和分页（历史发现）
【问题位置】`web/src/services/api/prompts.ts`、`server/lib/prompt-index.ts`
【历史行为】`fetchPrompts` 曾先调用 `fetchAllPrompts`，完成分类、搜索、标签和分页。
【历史影响】来源与提示词增加后，网络、IndexedDB、内存和分类计算会线性增长，低端手机首次搜索和切换标签容易变慢。
【整改状态】✅ 已完成。服务端建立标准化提示词索引，支持 `sourceId`、关键词、分类、标签、页码和页大小查询；公共模式前端不再因为索引响应而回退到浏览器全量抓取。索引未就绪时明确提示同步状态，而不是静默长时间加载。
【剩余风险】自定义来源脚本仍需继续收紧执行边界；来源数量增长后应增加定时同步、来源健康告警和索引重建监控。

## 【AUD-019】

【等级】🟠 P1
【类别】藏卷阁 / 数据规模
【问题名称】服务端返回全部资产，分页只发生在浏览器（历史发现）
【问题位置】`web/src/stores/use-asset-store.ts`、资产选择器和服务端资产列表接口
【历史行为】登录后曾一次加载用户全部资产并解析 URL，页面再按数量切片。
【历史影响】数百到数千资产后，全量 JSON、URL 解析、状态持久化和前端过滤会拖慢首次进入并增加内存。
【整改状态】✅ 已完成。登录首屏只请求服务端第一页；后续通过“加载更多藏卷阁/作品”按页读取并合并。公共模式下本地持久化资产不再启动时解析全部远程图片和视频地址；WebDAV 主动同步仍调用完整读取，避免同步遗漏。
【剩余风险】资产选择器目前仍以手动加载更多为主，后续可增加服务端搜索、类型和时间过滤，以及更轻量的专用缩略图 DTO。

## 【AUD-020】

【等级】🟡 P2，待验证
【类别】无限画布 / 大规模性能
【问题名称】缺少 300-1000 节点自动性能基准
【问题位置】`web/src/pages/canvas/project.tsx:686` 附近的可见节点、节点 Map 和连接筛选
【当前行为】已实现视口裁剪、缩略图、`React.memo` 和 RAF 拖动；但视口变化仍需扫描节点集合并重建部分 Map。
【为什么有问题】代码结构说明已做优化，但没有数据证明 300、500、1000 节点下拖动、缩放、框选和内存仍达标。
【实际影响】不能确认大型项目的性能上限，也无法防止后续回归。
【建议修改】增加 Playwright 性能场景：固定节点和连线数据，记录首次渲染、10 秒缩放拖动 FPS、主线程长任务、内存和 Undo 耗时；设定 100/300/500 三档预算。
【修改优先级】中。
【修改后收益】把“感觉不卡”变成可持续验证的性能指标。
【是否必须修改】不是上线阻断项，但必须保留“待验证”标签。

## 【AUD-021】

【等级】🟡 P2
【类别】部署 / 网络性能
【问题名称】生产域名未通过 Cloudflare 代理
【问题位置】生产 DNS 与 HTTP 响应头
【当前行为】生产响应由 `Via: 1.1 Caddy` 直接提供，没有 `CF-Ray`、`CF-Cache-Status`；DNS 解析到源站 IP。
【为什么有问题】当前不能使用 Cloudflare 的静态资源边缘缓存、WAF、源站隐藏和部分抗攻击能力。
【实际影响】异地访问延迟、静态资源回源、源站暴露和突发流量承受能力弱于已代理状态。
【建议修改】将业务域名记录开启 Cloudflare 橙云；只缓存带内容哈希的静态构建资源和公开缩略图，`/api`、用户资产、`/health` 保持不缓存；确认 WebSocket/SSE 和大文件上传规则。
【修改优先级】中高。
【修改后收益】静态加载、源站保护和全球访问稳定性改善。
【是否必须修改】不是功能上线阻断项，但建议上线初期完成。

---

# 九、安全问题

## 【AUD-022】

【等级】🟠 P1
【类别】任意脚本 / 供应链边界
【问题名称】自定义提示词来源脚本通过 `new Function` 在用户页面执行
【问题位置】`web/src/services/api/prompt-source-runtime.ts:169`
【当前行为】管理员配置的脚本文本在浏览器中动态编译，可访问页面运行环境；公网模式限制为 trusted 来源，但 trusted 的判断仍是配置边界。
【为什么有问题】一旦管理员误粘贴恶意脚本、来源被篡改或后台账号被接管，脚本可读取页面可访问的数据并发起网络请求。
【实际影响】可能导致前端数据泄露、会话内操作被劫持或供应链攻击。
【建议修改】将自定义来源抓取与解析迁到服务端隔离 Worker，提供受限 HTTP 客户端和结构化解析 API；短期至少禁止脚本访问 `window`、`document`、storage，并增加脚本哈希、审核状态和变更审计。
【修改优先级】第 9 位。
【修改后收益】管理员可配置能力保留，但不再把任意代码执行权交给每个用户浏览器。
【是否必须修改】正式开放第三方自定义来源前必须修改。

## 【AUD-023】

【等级】🟠 P1
【类别】账号安全
【问题名称】个人密码与 Session 管理缺少完整闭环
【问题位置】`server/index.ts:4611` 附近、`server/lib/auth.ts:26`
【当前行为】个人密码最低 6 位；Session 默认 14 天；没有用户自助修改/重置密码、单设备退出、全部设备退出或 token version。账号停用会在每次请求时即时生效，这是现有保护。
【为什么有问题】面向真实用户后，弱密码、设备丢失和凭据泄露都需要用户或管理员有明确撤销路径。
【实际影响】账号恢复成本高，泄露后的 14 天 Session 无法精确失效，只能停用整个账号或更换全局密钥。
【建议修改】最低密码提升至 8-10 位并检查常见弱密码；增加修改密码、管理员重置、`session_version`、全部设备退出和最近登录设备列表。
【修改优先级】第 10 位。
【修改后收益】形成可运营的账号安全闭环。
【是否必须修改】建议上线前至少完成修改密码与全部设备退出。

## 【AUD-024】

【等级】🟢 P3
【类别】CSP 配置
【问题名称】Chrome 会忽略无效的 IPv6 localhost CSP 源
【问题位置】`server/lib/security-policy.ts:1`
【当前行为】`connect-src` 包含 `http://[::1]:*`，Chrome 会将其判定为无效 source expression；其他 CSP 指令仍生效。
【为什么有问题】配置中存在浏览器不接受的表达式，容易掩盖本地 Agent 的 IPv6 兼容问题。
【实际影响】不影响 HTTPS、frame、script 等主要防护，但通过 IPv6 localhost 连接本地 Agent 可能失败并产生控制台警告。
【建议修改】删除该项或改用浏览器支持的明确端口白名单；生产环境按需下发 localhost connect-src。
【修改优先级】低。
【修改后收益】CSP 无警告、意图更清晰。
【是否必须修改】否。

---

# 十、功能缺口

## 【AUD-025】

【等级】🟡 P2
【类别】专业画布能力
【问题名称】缺少锁定、隐藏、图层、对齐和吸附
【问题位置】画布节点系统和侧边面板
【当前行为】有分组、层级顺序和多选，但没有节点锁定、隐藏、图层列表、对齐、等距分布和参考线吸附。
【为什么有问题】这些不是竞品装饰功能，而是节点超过十几个后降低误操作和排版成本的基础能力。
【实际影响】复杂海报、商品套图和多版本比较时容易误拖节点，精确布局耗时。
【建议修改】优先顺序为：锁定/隐藏 -> 多选对齐/分布 -> 吸附参考线 -> 图层面板；不要先做复杂 Photoshop 式图层混合。
【修改优先级】高。
【修改后收益】画布从“能组织”升级到“适合长期项目”。
【是否必须修改】第二阶段核心能力。

## 【AUD-026】

【等级】🟡 P2
【类别】创作历史 / 版本管理
【问题名称】缺少版本分支和结果比较
【问题位置】画布历史、生成结果和资产来源
【当前行为】画布只有线性 Undo；多次生成可形成多个节点，但没有“从此版本继续”“A/B 对比”“标记最终版”的统一结构。
【为什么有问题】AI 创作天然会产生分支，线性历史不能表达用户保留多个方向的需求。
【实际影响】用户依赖手动复制节点和命名，版本多后难以判断来源和优劣。
【建议修改】为资产和节点增加 `derivedFrom`、`variantGroupId`、`versionLabel`；提供并排/滑杆比较、设为主版本和从当前版本继续。
【修改优先级】中高。
【修改后收益】生成血缘成为产品可感知的核心差异化。
【是否必须修改】建议第三阶段完成。

## 【AUD-027】

【等级】🟡 P2
【类别】问道台 / 长期记忆
【问题名称】普通问道台没有通用摘要型长期记忆
【问题位置】`server/modules/chat/service.ts:13`、`:531`
【当前行为】普通对话默认发送最近 24 条、最多约 60,000 字符；超过上下文窗口后旧事实不再发送。斗气人生已有摘要、关键事实、最近事件和未完成目标。
【为什么有问题】角色聊天、项目咨询和长期创作会话会逐渐忘记用户偏好与关键决策。
【实际影响】用户需要反复解释背景，角色连续性和项目连续性下降。
【建议修改】复用斗气人生的轻量方案：会话摘要、关键事实、用户偏好、未完成事项和最近事件；由文本模型定期更新，可由用户查看、编辑和删除。暂不需要向量数据库。
【修改优先级】中高。
【修改后收益】长会话稳定，角色预设更有持续价值。
【是否必须修改】不是上线阻断项，但值得优先新增。

---

# 十一、数据架构问题

## 【AUD-028】

【等级】🟢 P3
【类别】本地模式 / 资源清理
【问题名称】本地模式 `cleanupImages` 为空实现
【问题位置】`web/src/stores/use-asset-store.ts` 的 `cleanupImages`
【当前行为】公网模式有服务端引用扫描、宽限期 GC 和文件清理；本地 Asset Store 的 `cleanupImages` 仅忽略参数。
【为什么有问题】纯本地模式长期使用时，IndexedDB 或本地持久化可能保留不再引用的旧图片。
【实际影响】主要影响非公网部署和开发模式，可能造成浏览器存储逐渐增长。
【建议修改】本地模式增加引用根扫描、宽限期和可视化清理；公网模式继续以服务端 GC 为准，避免双重删除。
【修改优先级】低。
【修改后收益】本地部署的数据生命周期完整。
【是否必须修改】否。

## 【AUD-029】

【等级】🟡 P2
【类别】数据库 / 数据保留
【问题名称】审计日志、登录日志和修炼账本缺少明确保留策略
【问题位置】`server/modules/cultivation/service.ts` 及数据库初始化
【当前行为】生成历史、聊天、灵彩、商品项目、斗气人生、任务和缓存均有数量或清理上限；审计日志、登录日志和修炼账本没有同等级的保留周期说明。
【为什么有问题】日志属于持续增长数据，长期运行后会增加 SQLite 体积和后台查询成本。
【实际影响】短期影响小，数月后可能导致备份变大、后台分页变慢和磁盘增长难以解释。
【建议修改】定义保留策略：登录日志 180 天、一般审计 365 天、额度账本保留汇总后归档；提供管理员导出和手动清理，清理过程写审计记录。
【修改优先级】中。
【修改后收益】数据库增长可预测，备份和运维成本可控。
【是否必须修改】上线后一个月内完成。

---

# 十二、AI 工作流问题

## 【AUD-030】

【等级】🟡 P2
【类别】渠道能力 / 历史迁移
【问题名称】旧模型缺少 `imageCapabilities` 时走宽松兼容规则
【问题位置】`web/src/stores/model-capabilities.ts`、服务端图像请求校验
【当前行为】新模型默认 `mode: auto` 且保守为 1:1、单参考图、单输出；但历史模型没有能力配置时，为兼容旧数据会走较宽松规则。
【为什么有问题】同一渠道中新旧模型的比例、参考图和数量限制可能表现不一致。
【实际影响】管理员误以为所有模型都受新能力表约束，部分旧模型仍可能提交上游不支持的参数。
【建议修改】提供一次管理员迁移审计：列出所有缺少能力配置的历史模型，要求选择自动识别、保守默认或手动配置；迁移后服务端拒绝无状态模型。
【修改优先级】中高。
【修改后收益】新增渠道和历史渠道使用同一严谨能力协议。
【是否必须修改】新渠道频繁增加前应完成。

## 已确认合理的 AI 工作流

- 生图通过服务端任务系统持久化，不依赖单次浏览器请求完成。
- 幂等 Key 避免重复点击造成重复任务。
- 额度先预占，成功结算，失败和部分失败按事务返还。
- 任务刷新可恢复，SADAI/UU 临时 URL 有延迟归档和恢复路径。
- 多图输出可拆为独立任务，单张失败不必抹掉全部成功结果。
- 前端能力选择后，服务端再次校验比例、质量、格式、参考图数量和输出数量。
- API Key 只保存在服务端并加密，浏览器不直接获得渠道密钥。

---

# 十三、修仙体系问题

当前修炼体系总体方向合理：它已经承担身份、额度、并发、高清、重绘、扩图和商品能力解锁，而不是只显示一个装饰等级。商品幻境中的境界能力差异最有实际意义。

需要调整的重点：

- 修炼权限必须从模型名猜测迁移到管理员显式能力配置，见 AUD-008。
- 普通画布中，用户应能直接看到“当前境界新增了什么能力”，而不是只看到数值变化。
- 境界不足时继续使用现有克制文案，不要弹出商城或诱导付费。
- 后台应提供“以某境界预览前台能力”的测试模式，防止管理员配置后无法验证。
- 继续保持修炼体系是身份和能力反馈，不增加排行榜、抽卡、商城和强制日常任务。

结论：**保留并强化能力解释，不继续游戏化扩张。**

---

# 十四、移动端问题

移动端不是“没有横向滚动”就等于可用。当前首页、丹青台、灵彩、商品幻境和藏卷阁能正常展示，但无限画布仅达到查看和有限操作级别。

已确认的问题：

- 节点主体拖动使用 `onMouseDown`，见 `web/src/components/canvas/canvas-node.tsx:406`。
- 四个缩放手柄使用鼠标事件，见 `canvas-node.tsx:476-479`。
- 连线手柄使用鼠标事件，见 `canvas-node.tsx:482-484`。
- 缺少双指缩放、触摸拖动模式和触摸连线替代交互。
- 问道台顶部和输入区拥挤，见 AUD-010。
- 藏卷阁首屏有效内容下沉，见 AUD-015。

建议将手机端定位为“轻量创作与审核”，而不是强行复制桌面画布：

1. 默认单指平移画布，长按选择节点。
2. 双指缩放，选中后用底部属性栏修改位置与尺寸。
3. 连线改为“选择来源 -> 选择连接目标”的两步模式。
4. 提供“性能模式”，隐藏连接动画、阴影、网格和非必要预览。
5. 平板保留接近桌面的操作，手机优先完成生成、预览、保存和简单排列。

---

# 十五、异常流程审查

| 异常场景 | 当前行为 | 审查结论 | 建议 |
|---|---|---|---|
| AI API 超时/失败 | 任务进入失败状态并返还额度 | ✅ 合理 | 保留可读上游错误和重试。 |
| AI 返回空结果 | 视为失败 | ✅ 合理 | 记录渠道 request ID 便于追查。 |
| 多图部分失败 | 成功结果保留，失败结果退款 | ✅ 合理 | UI 持续显示成功/总数。 |
| 连续点击生成 | 幂等与按钮状态共同限制 | ✅ 合理 | 增加请求已受理反馈即可。 |
| 生成中刷新 | 服务端任务可恢复 | ✅ 合理 | 保留任务中心恢复。 |
| 删除生成中节点 | 请求仍可能继续 | ⚠️ 不合理 | 见 AUD-002。 |
| 商品套图中刷新 | 尚未提交页面停止 | ⚠️ 不合理 | 见 AUD-003。 |
| 问道台切页 | 回答停止 | ⚠️ 不理想 | 见 AUD-004。 |
| 图片上传失败/损坏 | 有格式、MIME、签名、像素上限校验 | ✅ 合理 | 错误文案区分损坏、过大、不支持。 |
| 网络断开后恢复 | 任务和部分同步可恢复 | 🟡 部分合理 | 画布冲突必须保留副本。 |
| 多端同时编辑 | revision 冲突警告 | 🔴 不足 | 见 AUD-001。 |
| SQLite 写入失败 | 多数接口返回错误，任务有状态 | 🟡 | 增加磁盘只读/空间不足专用健康告警。 |
| 浏览器内存不足 | 无专用恢复 | 待验证 | 大图导出和大型画布增加性能模式。 |

---

# 十六、长期产品风险

1. **模块继续增加但资产关系不增强。** 最终会变成多个 AI 工具的导航站，而不是创作空间。
2. **用户作品增长快于数据分页和版本能力。** 藏卷阁、提示词库和画布都需要按规模设计。
3. **渠道能力依赖手工维护。** 必须把“文档支持什么”转成可审计配置，而不是继续增加字符串特判。
4. **单机部署缺少运营级可观测性。** 当前架构不需要拆微服务，但需要任务延迟、渠道成功率、磁盘、备份和数据库增长告警。
5. **修炼文案压过工具语义。** 东方风格应增强记忆点，不能让普通用户猜“藏宝阁、功法楼、太古遗迹”分别是什么。
6. **本地浏览器状态与服务端状态并存。** 需要继续减少双写和冲突，优先让服务端成为用户资产与任务的事实来源。
7. **第三方来源和渠道是供应链。** 提示词脚本、模型能力和上游返回都需要审计、隔离和降级。

---

# 十七、建议新增功能

以下建议只选择能强化当前定位的功能，不按竞品清单机械补齐。

## 1. 冲突副本与版本恢复

【功能名称】画布冲突副本与版本恢复
解决的问题：多端同步冲突可能丢失本地修改。
为什么值得做：创作数据可信度是所有后续功能的基础。
用户场景：用户在电脑 A 修改后，又在电脑 B 打开旧页面继续编辑。
核心交互：自动保存冲突副本；显示本地版与云端版时间、节点数和预览；允许恢复或导出。
实现难度：中。
产品价值：极高。
优先级：P0。
是否建议现在开发：是，立即开发。

## 2. 服务端商品套图批任务

【功能名称】商品套图后台任务
解决的问题：七张套图依赖前端页面持续打开。
为什么值得做：这是商品幻境从演示功能走向可靠工具的关键。
用户场景：用户上传商品，确认方案后离开页面，稍后回来查看逐页结果。
核心交互：一次提交、后台排队、逐页进度、失败单页重试、完成通知。
实现难度：中高，可复用现有任务系统。
产品价值：高。
优先级：P1。
是否建议现在开发：是。

## 3. 通用会话摘要记忆

【功能名称】问道台长期记忆
解决的问题：长对话超过上下文后遗忘项目背景和用户偏好。
为什么值得做：问道台已有角色和长期使用场景，记忆比继续增加预设更有价值。
用户场景：用户连续数周讨论同一项目，希望 AI 记住技术栈、审美和未完成事项。
核心交互：自动摘要；“记忆”抽屉展示事实、偏好和待办；用户可编辑、删除、暂停记忆。
实现难度：中，可复用斗气人生结构。
产品价值：高。
优先级：P1/P2。
是否建议现在开发：是，在稳定性修复后开发。

## 4. 服务端提示词聚合与分页

【功能名称】提示词内容索引服务
解决的问题：首次加载等待外部来源，浏览器全量聚合。
为什么值得做：功法楼内容会持续增长，必须从“抓取页面”变成产品数据。
用户场景：用户首次打开就看到精选内容，搜索和分类立即响应。
核心交互：后台显示来源健康、上次同步和数量；前台按页加载，慢来源不阻塞。
实现难度：中。
产品价值：高。
优先级：P1。
是否建议现在开发：是。

## 5. 藏卷阁服务端分页与搜索

【功能名称】资产增量加载
解决的问题：全部资产一次进入浏览器，规模增大后首屏变慢。
为什么值得做：作品库会自然增长，是最确定的数据规模问题。
用户场景：用户拥有上千张图片，仍可按最近、来源、标签快速查找。
核心交互：无限滚动或分页；搜索、标签、类型、时间由服务端过滤；首屏只取缩略图。
实现难度：中。
产品价值：高。
优先级：P1。
是否建议现在开发：是。

## 6. 画布锁定、隐藏、对齐与吸附

【功能名称】专业画布基础排版
解决的问题：复杂画布容易误操作，精确排列效率低。
为什么值得做：这些能力直接服务已有节点和商品套图，不是额外模块。
用户场景：用户把主图和六张详情页按顺序排列，并锁定参考图。
核心交互：多选后对齐/分布；节点锁定/隐藏；拖动显示参考线和间距。
实现难度：中。
产品价值：高。
优先级：P2。
是否建议现在开发：是，分两次交付。

## 7. 版本分支与图片比较

【功能名称】创作版本树
解决的问题：AI 多次生成后的方向、来源和最终版难以管理。
为什么值得做：生成血缘是当前项目最有潜力的差异化。
用户场景：用户从同一参考图生成三个方向，比较后选择一个继续扩图。
核心交互：自动形成变体组；并排或滑杆比较；设为主版本；从当前版本继续。
实现难度：中高。
产品价值：高。
优先级：P2。
是否建议现在开发：是，在数据模型稳定后开发。

## 8. AI 画布上下文助手

【功能名称】问道台读取选中画布
解决的问题：问道台与画布目前是并列模块，AI 不理解当前创作现场。
为什么值得做：能直接强化跨模块联动，而不是再加一个孤立 Agent。
用户场景：用户选中三张参考图和一张结果图，询问“保持人物一致，下一张该怎么写提示词”。
核心交互：在画布侧边打开问道台；明确列出已附加节点；AI 可建议但执行任何画布修改前必须确认。
实现难度：中高。
产品价值：极高。
优先级：P2。
是否建议现在开发：是，第三阶段开发。

## 9. 移动端触控模式

【功能名称】手机和平板触控画布
解决的问题：鼠标事件使移动画布只能有限使用。
为什么值得做：用户经常在手机查看结果、调整顺序和继续生成。
用户场景：用户在手机上查看生成结果，拖动节点、双指缩放并保存。
核心交互：单指平移、长按选择、双指缩放、底部属性栏、两步连线。
实现难度：高。
产品价值：中高。
优先级：P2。
是否建议现在开发：平板优先，手机先做轻量模式。

## 10. 运营与渠道健康中心

【功能名称】渠道成功率与任务延迟监控
解决的问题：上游慢、扣费成功但回调失败、归档延迟等问题需要靠人工截图排查。
为什么值得做：多渠道是优势，也是主要稳定性来源。
用户场景：管理员快速判断某渠道最近一小时成功率、P50/P95 耗时、归档失败和退款情况。
核心交互：按渠道和模型查看提交、成功、失败、超时、恢复、平均耗时；异常阈值告警。
实现难度：中。
产品价值：高。
优先级：P1/P2。
是否建议现在开发：是，先做后台指标，不做复杂 APM。

---

# 十八、建议删除或合并功能

## 建议删除

1. **删除灵彩无效 `curves` 数据字段。** 当前 UI 和渲染都不支持，保留只会制造半成品状态。
2. **删除生产 CSP 中无效的 `http://[::1]:*` 表达式。** 或换成明确可用配置。
3. **删除前台技术仓库名作为主要来源标题。** 技术信息保留在详情和后台。

## 建议改名

1. **“AI 炼彩”改为“智能炼彩”或“自动优化”。** 直到真正接入视觉模型。
2. **修仙名称保留，但同时给出功能副标题。** 例如“功法楼 · 提示词库”“藏卷阁 · 作品资产”，避免新用户猜测。

## 建议合并

1. **统一所有图片结果动作。** 丹青台、商品幻境、画布和灵彩共用“进入画布 / 灵彩处理 / 保存藏卷阁 / 下载”操作组件。
2. **统一任务状态入口。** “正在生成”负责实时任务，“太古遗迹”负责已结束记录，避免页面内再出现重复的“本次结果”区域。
3. **统一资产选择器。** 丹青台参考图、商品上传、灵彩源图和问道台图片附件应共用同一藏卷阁选择组件。

## 可以暂时保持

- Bun + React + SQLite 单体架构。
- 本机备份 14 份；当前只有一台服务器时，不强制引入复杂异地灾备，但必须定期验证备份可恢复。
- 修炼体系的境界名称、额度和能力反馈。
- 灵彩浏览器本地调色路线；不要为了“AI”强行把全部图片上传到后端。
- 手动将生成结果保存到藏卷阁，避免自动产生大量无价值资产。

---

# 十九、暂时不要做的功能

1. 商城、抽卡、排行榜、宗门社交和强制每日任务。
2. 实时多人协作。先解决冲突副本和版本恢复，再评估协作。
3. 插件市场。当前自定义脚本边界尚未完全隔离，不适合扩大供应链面。
4. 大型 3D、WebGL 粒子和境界专属重动画。
5. 浏览器内完整 RAW、图层混合和专业印前工作流。
6. 为所有 AI 模型统一展示 seed、steps、CFG、Negative Prompt 等高级参数。
7. 复杂向量数据库记忆。先实现可见、可编辑的摘要和关键事实。
8. 店铺、订单、库存、投放和完整电商 ERP。
9. 为了“微服务化”拆分当前单体。现阶段拆分成本高于收益。
10. 没有明确用户场景的更多独立创作模块。

---

# 二十、最应该马上修改的 10 项

| 排名 | 项目 | 等级 | 价值/成本判断 |
|---|---|---|---|
| 1 | 画布冲突自动保存副本与版本恢复 | P0 | 直接消除数据丢失路径，成本中。 |
| 2 | 删除生成中节点同步取消任务 | P1 | 减少无效扣费和状态错乱，成本低到中。 |
| 3 | 商品套图迁移为服务端批任务 | P1 | 修复核心业务可靠性，成本中高。 |
| 4 | 灵彩大图导出迁移 Worker | P1 | 避免页面冻结，成本中。 |
| 5 | 提示词库首屏增量加载/服务端聚合 | 已完成 | 已落地服务端索引和按页读取；后续补来源健康告警。 |
| 6 | 藏卷阁服务端分页与搜索 | 部分完成 | 已落地按页加载；服务端搜索、过滤和轻量 DTO 留作后续迭代。 |
| 7 | 问道台支持切页后台继续回答 | P1 | 提升跨模块使用效率，成本中。 |
| 8 | 空画布三项首步入口 | P1 | 显著降低新用户使用门槛，成本低。 |
| 9 | 自定义提示词脚本迁出浏览器全局 | P1 | 收紧供应链安全边界，成本中高。 |
| 10 | 修改密码、全部设备退出和 Session 版本 | P1 | 补齐真实用户账号安全，成本中。 |

---

# 二十一、最值得新增的 10 个功能

| 排名 | 功能 | 推荐阶段 |
|---|---|---|
| 1 | 冲突副本与版本恢复 | 阶段 1 |
| 2 | 服务端商品套图批任务 | 阶段 1 |
| 3 | 通用会话摘要记忆 | 阶段 2 |
| 4 | 服务端提示词聚合与分页 | 已完成，后续补来源健康 |
| 5 | 藏卷阁服务端分页与搜索 | 分页已完成，搜索过滤后续补齐 |
| 6 | 画布锁定、隐藏、对齐与吸附 | 阶段 2 |
| 7 | 版本分支与图片比较 | 阶段 3 |
| 8 | AI 画布上下文助手 | 阶段 3 |
| 9 | 移动端触控模式 | 阶段 2-3 |
| 10 | 渠道健康与任务延迟中心 | 阶段 1-2 |

---

# 二十二、产品评分

| 维度 | 分数 | 说明 |
|---|---:|---|
| 产品完整度 | 81/100 | 生成、画布、加工、资产、聊天和商品链路均已存在。 |
| UI | 75/100 | 单模块完成度较好，但跨模块视觉基线不统一。 |
| UX | 72/100 | 核心流程可用；首次画布、移动端、长任务和异常恢复仍有成本。 |
| 无限画布 | 79/100 | 核心交互、裁剪、Mini Map、缩略图和生成节点较完整；缺专业排版与可靠冲突恢复。 |
| AI 工作流 | 84/100 | 任务、幂等、退款、恢复和能力校验是明显优势。 |
| 专业创作能力 | 73/100 | 跨模块加工有基础；版本、图层、锁定、吸附和局部编辑不足。 |
| 普通用户友好度 | 69/100 | 东方命名有特色，但空画布和多模块下一步仍需解释。 |
| 性能 | 74/100 | 已有懒加载、预取、视口裁剪和缩略图；全量数据与大图主线程处理需改善。 |
| 稳定性 | 82/100 | 466 项测试通过，任务与额度链路稳健；多端冲突和前端套图长任务是主要缺口。 |
| 安全 | 78/100 | 资源隔离、上传校验、密钥加密和安全头较好；动态脚本和账号闭环需处理。 |

---

# 二十、整改复审（2026-08-23）

本节是对本报告前述整改项的最新状态说明，优先级和结论以本节为准。原报告保留作为上线前审查基线，不删除历史判断，便于追溯问题是如何被发现和修复的。

## 已完成的关键整改

- **AUD-001 画布冲突副本：已修复。** 远端 revision 冲突时会自动保留本地冲突副本，并提供“打开本地副本”“查看云端版本”“导出本地副本”三个明确动作；导出模块按需加载，不增加首屏包体。该问题从 P0 数据丢失路径降为需多设备人工回归验证的 P1 风险。
- **AUD-002 生成中节点删除：已接入取消链路。** 删除生成中节点时会停止前端请求并请求服务端取消，避免结果继续回填；仍需使用真实上游渠道验证不可取消的上游任务不会再次写回节点。
- **AUD-003 商品套图长任务：已迁移为服务端批任务。** 商品幻境一次提交后由服务端持久化每一页的状态、结果和失败原因，刷新、离开页面后可恢复，支持取消和仅重试失败项；前端不再依赖页面内顺序循环维持整套生成。
- **问道台跨页面状态：已改善。** 站内切页时保留回答中的全局运行状态，顶部导航显示回答中/停止中并可返回问道台；用户消息立即显示、回答过程与停止状态同步。浏览器刷新、关闭页面或服务重启后的完整续答仍需服务端 Chat Run 才能实现，不能把当前能力误判为后台持续回答。
- **渠道监控：已增加 P95 耗时。** 运营侧可以区分平均耗时与尾部延迟，便于识别 SADAI 等渠道的偶发慢请求。
- **画布移动端：已补充双指缩放和图层语义。** 这改善了移动端查看与基础操作，但不等同于完整触摸编辑器，节点拖动、连线和复杂排版仍应按移动端专项清单回归。
- **提示词库服务端索引：已完成。** 管理员来源在服务端执行并预热索引，前端按页读取；来源索引失败会留下状态记录，删除来源时同步清理索引，避免慢来源阻塞整个首屏。
- **藏卷阁按需加载：已完成。** 登录首屏只读取第一页并延迟解析远程媒体地址，用户明确点击加载更多时再获取后续页；WebDAV 主动同步保留完整读取路径。

## 当前上线结论

本轮整改后，原报告中唯一明确的 P0 数据丢失路径已经有可恢复副本保护；商品套图和问道台跨页体验的主要可靠性缺口已收敛；提示词库与藏卷阁的首屏全量加载问题也已改为服务端索引和按需分页。结合本轮重新通过的类型检查、全量测试和生产构建，项目可以进入“有条件正式上线”阶段。

上线前仍必须人工确认：

1. 真实生产环境多设备同时编辑时，冲突副本三按钮均能打开正确版本，且不会重复创建副本。
2. 商品批任务在刷新、离开、取消、部分失败和仅重试失败项时，额度、任务状态和结果数量一致。
3. 真实文本渠道的问道台流式完成、提前断流、停止和重新进入行为符合待测试清单；不要把站内切页保活当成服务端后台续答。
4. 部署后 `/health` 返回当前提交版本，生产日志没有新增未处理异常，SQLite 备份与磁盘空间仍正常。

## 复审后的分级

- **必须阻止上线的问题：** 暂未发现新的代码级 P0；若上述三类真实环境回归失败，应暂停对外开放对应功能。
- **建议上线首周观察：** 商品批任务尾部耗时、渠道 P95、取消成功率、画布冲突副本数量、问道台流式断连率和磁盘增长。
- **可以后续处理：** 服务端 Chat Run、提示词来源定时同步与健康告警、藏卷阁服务端搜索和轻量缩略图 DTO、灵彩 Worker 导出、完整触摸编辑器、账号修改密码与全部设备退出。
| 商业产品成熟度 | 71/100 | 已能服务真实用户，但运维指标、账号恢复、数据规模和统一体验尚未完全产品化。 |

综合评分：**76/100**

当前阶段：

> **接近“可公开使用的完整工具”，正在从可用工具向成熟商业产品过渡。**

不是原型，也还不能称为成熟商业化产品。修复 P0 后可以正式开放，但应控制首批用户规模，持续观察渠道成功率、任务耗时、资产增长和数据库备份。

---

# 二十三、五阶段产品路线图

## 阶段 1：修复核心问题

目标：让用户敢于把真实创作数据放进产品。

| 任务 | 优先级 | 复杂度 | 依赖关系 |
|---|---|---|---|
| 冲突副本与最近版本恢复 | P0 | 中 | 先确定项目 revision 快照结构。 |
| 删除节点同步取消生成 | P1 | 中 | 复用现有任务取消接口。 |
| 商品套图服务端批任务 | P1 | 中高 | 复用任务队列、额度和归档。 |
| 灵彩 Worker 导出 | P1 | 中 | 定义 Worker 消息与取消协议。 |
| 账号修改密码、全部设备退出 | P1 | 中 | 增加 session version。 |
| 渠道成功率和任务耗时基础指标 | P1 | 中 | 复用任务和用量日志。 |

## 阶段 2：补齐核心能力

目标：让长期使用不会随着作品和节点增多而变慢或混乱。

| 任务 | 优先级 | 复杂度 | 依赖关系 |
|---|---|---|---|
| 藏卷阁服务端搜索、标签、轻量 DTO | P1 | 中 | 已有分页基础，继续补服务端过滤和缩略图数据。 |
| 提示词来源定时同步、分页和健康告警 | P1 | 中 | 已有服务端索引，继续补调度、失败告警和脚本隔离。 |
| 节点锁定、隐藏、对齐、分布 | P2 | 中 | 统一多选命令和 Undo 事务。 |
| 空画布首步入口和统一结果动作 | P1/P2 | 低 | 统一跨模块动作组件。 |
| 通用聊天摘要记忆 | P2 | 中 | 复用斗气人生记忆结构。 |
| 手机/平板基础触控模式 | P2 | 高 | 统一 Pointer Events。 |

## 阶段 3：强化差异化

目标：把“生成血缘和跨模块继续创作”变成可感知优势。

| 任务 | 优先级 | 复杂度 | 依赖关系 |
|---|---|---|---|
| 版本分支、变体组、A/B 比较 | P2 | 中高 | 资产血缘字段和版本恢复。 |
| 问道台读取选中画布上下文 | P2 | 中高 | 统一资产引用与授权确认。 |
| AI 画布整理与提示词建议 | P2 | 高 | 先完成对齐、分组和 Undo 命令。 |
| 商品套图一键进入画布继续排版 | P2 | 中 | 服务端套图任务和变体组。 |
| 跨模块最近操作与项目工作区 | P2 | 中 | 统一资产来源和项目 ID。 |

## 阶段 4：商业化准备

目标：不是先上会员，而是先具备可运营、可支持、可恢复的能力。

| 任务 | 优先级 | 复杂度 | 依赖关系 |
|---|---|---|---|
| 备份恢复演练、磁盘和数据库告警 | P1 | 中 | 明确保留周期。 |
| 管理员渠道 SLA、成本和异常告警 | P1 | 中 | 完整指标采集。 |
| 用户数据导出、注销与删除流程 | P1/P2 | 中 | 资源引用和 GC。 |
| 服务条款、隐私说明、内容申诉流程 | P1 | 中 | 结合实际运营地区。 |
| Cloudflare 代理和静态缓存策略 | P2 | 中 | 验证 SSE、上传和私有资产不缓存。 |
| 小规模计费试验 | P2 | 中高 | 先保证额度账本、退款和模型成本准确。 |

## 阶段 5：长期 AI Agent 化

目标：AI 理解用户的项目和画布，但任何修改都可预览、可撤销、可确认。

| 任务 | 优先级 | 复杂度 | 依赖关系 |
|---|---|---|---|
| 项目级创作目标与长期记忆 | P2 | 高 | 会话摘要、项目工作区。 |
| AI 读取画布结构并提出操作计划 | P2 | 高 | 稳定的画布命令 API。 |
| 用户确认后执行整理、生成和版本分支 | P2 | 高 | 全部命令必须支持事务 Undo。 |
| 工作流模板沉淀 | P3 | 高 | 先验证真实重复场景。 |
| 个性化创作偏好 | P3 | 高 | 可见、可编辑、可删除的记忆体系。 |

---

# 二十四、最终产品方向

## 为什么用户愿意第一次使用

- 多渠道 AI 生图和参考图能力集中在一个界面。
- 生成后可立即进入无限画布组织和继续创作。
- 中文东方视觉与修炼身份有记忆点。

## 为什么用户愿意第二次回来

当前答案还不够强。真正的复访动力应来自：

- 作品、提示词、角色、商品项目和画布都被持续保存。
- 能从上次版本继续，而不是重新上传和解释。
- 问道台记住项目目标，画布保留生成血缘。
- 藏卷阁越用越有价值，但仍能快速找到内容。

## 核心产品原则

1. 每个新增模块必须能消费现有资产，并把结果继续交给其他模块。
2. 每个 AI 操作必须有任务状态、失败恢复、额度解释和结果血缘。
3. 每个自动操作必须可预览、可取消、可撤销。
4. 修炼体系只解释身份和能力，不替代清晰的工具语言。
5. 数据可靠性优先于动画、更多模板和更多角色。

---

# 二十五、最终上线建议

是否建议上线：**修复 AUD-001 后，建议以首批真实用户、小规模公开的方式上线。**

上线前必须完成：

1. 画布冲突自动保存副本，消除不可恢复覆盖路径。
2. 做一次真实备份恢复演练，而不只是确认备份文件存在。
3. 确认管理员密码、渠道 Key、Session Secret、加密 Secret 均不是默认值并已离开仓库。
4. 为磁盘不足、数据库备份失败和任务队列异常建立最小告警。

建议上线前完成：

1. 删除生成节点同步取消任务。
2. 增加修改密码和全部设备退出。
3. 收紧自定义提示词脚本执行边界。
4. 修复灵彩大图导出主线程冻结风险。

上线后重点观察：

- 各渠道成功率、提交到结果的 P50/P95 时间、归档恢复次数。
- 额度预占、成功结算、退款和上游实际扣费之间的差异。
- 藏卷阁资产数量、数据库大小、备份大小和磁盘增长。
- 画布同步冲突次数和版本恢复使用情况。
- 提示词来源失败率和首次可用时间。
- 移动端跳出率、问道台长回答停止率、商品套图中断率。

最终判断：

> 当前项目已经具备真实产品基础。下一阶段最重要的不是继续增加入口，而是把数据可靠性、长任务、规模化加载、跨模块资产和生成血缘做深。只要守住这一方向，“无限画境”有机会形成区别于普通 AI 生图站的长期价值。
